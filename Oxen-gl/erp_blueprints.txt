===============================================================================
                       ERP DEVELOPMENT BLUEPRINTS & PROMPTS
===============================================================================

-------------------------------------------------------------------------------
PROMPT 1: MULTI-GENERATION HIERARCHICAL COA & COST CENTERS (BACKEND INTERCEPTOR)
-------------------------------------------------------------------------------
Please upgrade our current FastAPI + SQLite ERP system architecture to support an advanced, multi-generation hierarchical Chart of Accounts (COA) and hierarchical Cost Centers (supporting Father ➔ Child ➔ Grandchild ➔ Great-Grandchild ➔ Leaf nodes) with automated recursive financial rollups. 

CRITICAL CONSTRAINT: Do not alter or break the existing UI layout contract. The frontend client application must make the same API fetch routing calls and receive the exact same JSON key-value object contracts it currently relies on. All advanced structural operations and tree aggregations must be handled entirely in the backend translation/shaping layer.

Please apply the following design changes to our codebase:

1. SQLITE DATABASE SCHEMA UPGRADES:
- Modify 'chart_of_accounts' to use a self-referencing hierarchy by adding a 'parent_code' column (TEXT) linking back to 'account_code'. Add an 'is_postable' boolean column (INTEGER 0 or 1). Summary parent accounts get 0; only deep leaf/grandchild nodes get 1.
- Modify 'cost_centers' to mirror this self-referencing hierarchy with 'code', 'name', 'parent_cc', and 'is_leaf' columns.
- Ensure 'journal_lines' supports a 'cost_center_id' column that links directly to a leaf node cost center.

2. FASTAPI CODE CONSTRAINTS & BUSINESS RULES:
- Use strict Pydantic payload interception. Block any attempt to post transaction entries directly to a structural summary parent account (where is_postable = 0).
- If an operational transaction hits an "Expense" or "Revenue" type account classification, enforce a strict business rule validating that a valid, leaf-node 'cost_center_id' is present in the payload line.
- Maintain the CEO Approval Workflow routing pipeline intact: entries automatically generated by background operations (e.g., adding a trip) must save to the database under a status of 'PENDING_CEO_APPROVAL'.

3. AUTOMATED RECURSIVE ACCOUNT ROLLUPS (SQLITE RECURSIVE CTE):
- Implement an optimized backend lookup function using a SQLITE "WITH RECURSIVE" Common Table Expression (CTE). 
- When an account code balance is queried, the query must recursively walk down through unlimited nested generation levels (Gen 1 to Gen 5+), discover all active leaf node child postings beneath that tree branch, and aggregate the balances upwards.
- Keep the outbound response model mapped identically to our old frontend format (e.g., returning flat attributes like 'balance', 'total_debit', 'total_credit') so the UI dashboard charts and tables process the payload smoothly without modifications.

Please write the updated database initialization script and the robust FastAPI endpoint configurations to complete this upgrade.


-------------------------------------------------------------------------------
PROMPT 2: AUTOMATED INVENTORY HOOK WITH COGS MATCHING (OPERATION LIFE CYCLE)
-------------------------------------------------------------------------------
Please write the complete FastAPI backend implementation for our automated Inventory Module Integration. This code must capture warehouse activities and seamlessly generate matching entries inside our new multi-generation, cost-center-validated General Ledger.

The integration must adhere to the following business rules and data constraints:
1. Operational Webhook Event: Create a POST endpoint at `/api/inventory/sales-hook` that accepts warehouse payloads containing 'trip_id', 'item_sku', 'quantity', 'unit_cost_price', 'unit_sale_price', and an assigned 'cost_center_id'.
2. Automated Ledger Splitting: Based on the matching accounting principle, the hook must automatically calculate financial totals and split the transaction into a precise 4-line double-entry voucher matrix:
   - Line 1 (Revenue Asset): DEBIT Main Bank Cash Account (e.g., '1101') ➔ cost_center_id = NULL.
   - Line 2 (Sales Revenue): CREDIT Sales Revenue Earnings Leaf Account ➔ cost_center_id = [Assigned Leaf Route CC].
   - Line 3 (COGS Expense): DEBIT Cost of Goods Sold Expense Leaf Account ➔ cost_center_id = [Assigned Leaf Route CC].
   - Line 4 (Inventory Reduction Asset): CREDIT Warehouse Stock Inventory Leaf Account ➔ cost_center_id = NULL.
3. Strict Schema Verification: Pass this auto-generated voucher through our Pydantic validation filters. The validation must confirm that:
   - Total Debits perfectly equal Total Credits.
   - Expense and Revenue lines explicitly include a Grandchild/Leaf Cost Center ID.
   - Balance-sheet asset tracking lines explicitly omit the Cost Center ID (keeping it NULL).
4. CEO Workflow Quarantine: Save this complete 4-line entry into the SQLite 'journal_headers' and 'journal_lines' tables with a state flag of 'PENDING_CEO_APPROVAL', holding it securely until it is authorized for final ledger posting.

Please output the clean, fully-commented FastAPI route code structure so we can integrate it seamlessly into our backend app file.


-------------------------------------------------------------------------------
PROMPT 3: INTER-COMPANY MULTI-BRANCH CONSOLIDATION ENGINE
-------------------------------------------------------------------------------
Please implement the Inter-Company Data Sync Engine in our FastAPI backend to handle transactions spanning multiple branches or separate companies, keeping our front-end contracts intact.

Please write the execution logic and endpoints following these constraints:
1. Multi-Company Clearing Accounts: Ensure our database setup includes specific clearing account structures to track inter-company trade: '1300' (Intercompany Receivables), '2300' (Intercompany Payables), '4100' (Intercompany Revenue), and '5100' (Intercompany Expenses).
2. Balanced Cross-Branch Webhook: Create an endpoint at `/api/operations/intercompany-trade`. When an action affects two different companies (e.g., Company A handles logistics for Company B), the backend must generate a balanced, combined 4-line journal entry entry matrix:
   - Company A (Origin): Debits Intercompany Receivables ('1300') and Credits Intercompany Revenue ('4100') linked to an active leaf cost center.
   - Company B (Target): Debits Intercompany Expense ('5100') linked to an active leaf cost center and Credits Intercompany Payables ('2300').
3. Multi-Entity Balance Check: The backend must explicitly confirm that the global sum of debits perfectly equals the global sum of credits across all involved database fields before accepting the voucher, saving it under status = 'PENDING_CEO_APPROVAL'.

Please output the clean Python SQLAlchemy data mapping structures and verification logic.


-------------------------------------------------------------------------------
PROMPT 4: TAX (VAT) CALCULATION ENGINE
-------------------------------------------------------------------------------
Please build an automated Tax Engine Module in our FastAPI + SQLite backend. This engine must intercept transactions to calculate and record Value Added Tax (VAT) dynamically on trip operations, rolling the balances into dedicated multi-generational liability accounts.

Please implement the following changes in our codebase:
1. CHART OF ACCOUNTS UPGRADE:
- Ensure the database initialization script includes dedicated tax accounts within our multi-generation tree:
  * "2000" -> Liabilities (Gen 1: Father, is_postable = 0)
  * "2200" -> Tax Payables (Gen 2: Child, is_postable = 0)
  * "2201" -> VAT Collected Liability (Gen 3: Grandchild/Leaf-Postable, is_postable = 1)
2. CORE TAX ENGINE INTERCEPTOR LOGIC:
- Create a reusable utility function `calculate_vat(gross_amount: Decimal, tax_rate: Decimal = 0.15)` that determines tax splits using a standard 15% VAT rate.
- Update the operational `/api/operations/add-trip` pipeline so that when a clerk inputs a gross revenue transaction, the backend automatically extracts the tax slice:
  * Tax Amount = Gross Amount - (Gross Amount / (1 + Tax Rate))
  * Net Revenue = Gross Amount - Tax Amount
- Modify the auto-generated 4-line journal matrix into an expanded 5-line balanced matrix before saving with Pydantic verification:
  * Line 1 (Asset): DEBIT Main Bank Cash Account for the Full Gross Amount.
  * Line 2 (Revenue): CREDIT Sales Revenue Earnings Leaf Account for the Net Revenue portion (Mapped to Leaf Cost Center).
  * Line 3 (Liability): CREDIT VAT Collected Liability Account ('2201') for the calculated Tax Amount.
  * Lines 4 & 5: Maintain our standard matching COGS Expense and Inventory Asset adjustment lines untouched.
3. DATA SHAPING COMPATIBILITY:
- Ensure the final output array passed downstream to the CEO approval queue translates all numbers to standard floats, keeping the multi-line array structurally identical to what our frontend components are already hardcoded to render on screen.

Please provide the updated database configuration and FastAPI integration code.


-------------------------------------------------------------------------------
PROMPT 5: CUSTOMER INVOICING WITH CRYPTOGRAPHIC CEO SIGN & STAMP WORKFLOW
-------------------------------------------------------------------------------
Please build out the backend business logic and PostgreSQL queries to link our existing Customer Invoice Management and Electronic Signature system with our multi-generation General Ledger. When the CEO electronically signs and stamps an invoice, the backend must instantly lock the invoice and execute a balanced, production-grade financial ledger posting.

Please implement the following changes in our codebase:
1. INVOICE SCHEMA SYNCHRONIZATION:

Please build an advanced Multi-Currency Bank Reconciliation Engine in our FastAPI + SQLAlchemy + SQLite/PostgreSQL backend package. This engine must handle clearing internal multi-currency ledger entries against imported bank statements, automatically converting and matching transaction values across different currencies.

Please apply the following changes to our codebase:

1. RECONCILIATION DATA TYPE EXTENSION:
- Ensure our 'JournalLine' table schema tracking includes 'reconciliation_status' (String, default='UNRECONCILED') and 'reconciled_at' (DateTime, Nullable).
- Create a Pydantic incoming validation model 'BankStatementRow' that accepts: 'bank_date' (String/Date), 'bank_reference' (String matching our internal reference_id or entry_number), 'bank_amount' (Decimal/Float), and 'bank_currency' (String, e.g., 'EUR', 'USD').

2. MULTI-CURRENCY CROSS-MATCHING LOGIC (`POST /api/finance/bank-reconciliation/multi-currency`):
- Restrict this endpoint to 'ADMIN' or 'CEO' roles only using our JWT security middleware.
- Loop through the array of incoming bank statement rows and query our ledger lines:
  * Locate the corresponding 'JournalLine' where the parent journal's identifier matches the 'bank_reference', the account code is our postable bank asset leaf ('1101'), and the status is fully posted/approved.
  * If the 'bank_currency' matches the internal journal's original source currency, match 'bank_amount' directly against 'source_debit' or 'source_credit'.
  * If the 'bank_currency' differs (e.g., matching a EUR bank row against a USD internal transaction), use the internal exchange rate stored on the journal header to calculate the expected conversion:
    Expected Conversion = bank_amount * journal_entry.exchange_rate
    Match this calculated conversion value directly against our stored 'base_debit' or 'base_credit' fields (allowing a tiny rounding tolerance of 0.01).
- If a perfect match is found, mutate that specific database line's status to 'RECONCILED' and stamp the current date.

3. GRACEFUL FAULT TOLERANCE:
- Do not fail or roll back the entire batch if an exception occurs. If a bank row does not match or contains an arithmetic discrepancy, add it to an 'unmatched_exceptions' data list.
- Return a clean flat object back to our UI, casting all values to ordinary floats:
  { "total_processed": int, "reconciled_count": int, "exceptions_count": int, "unmatched_details": list }

Please provide this updated implementation using our relative package structure paths and include a focused unit test.

Please write a comprehensive guide and a step-by-step setup manual detailing exactly how we must configure our GitHub Repository Secrets to wire up our newly created '.github/workflows/deploy.yml' CI/CD automated deployment playbook.

Please outline the precise steps, security contexts, and text formatting required to securely save our cloud infrastructure credentials so the GitHub runner can connect to our server (such as AWS, DigitalOcean, or Azure) via SSH:

1. EXPLICIT EXPLANATION OF REPOSITORY SECRETS:
- Explain where to find the secrets panel in the GitHub repository UI (Settings > Secrets and variables > Actions > New repository secret).
- Document the exact naming conventions and variable formatting required for all 5 variables used in our workflow:
  * SSH_HOST: The public static IPv4 address or domain name of our remote cloud server.
  * SSH_USERNAME: The root or administrative user account name (e.g., 'root', 'ubuntu', or 'ec2-user').
  * SSH_PORT: The custom network port assigned to our cloud server's SSH daemon (defaults to '22').
  * DEPLOY_PATH: The absolute folder path directory on our cloud server where our repository is cloned and where our docker-compose.yml file sits (e.g., '/home/ubuntu/app/').
  * SSH_PRIVATE_KEY: The complete, raw, unencrypted OpenSSH private key text (including the '-----BEGIN OPENSSH PRIVATE KEY-----' and '-----END OPENSSH PRIVATE KEY-----' boundary lines).

2. REMOTE MACHINE PRE-FLIGHT REQUISITES:
- Provide the short command-line entries we need to run once on our target cloud server terminal via SSH to prepare the folder structure and verify that Docker, Docker Compose, and Git are ready before triggering the first 'git push' automated release pipeline.

Please provide a clean, highly scannable Markdown guide that we can reference in our workspace.

Please write an updated Python simulation testing script using the 'requests' library that we can execute from our VS Code terminal window to audit our complete end-to-end multi-currency, audited, index-optimized, and closed accounting ecosystem.

The simulation script should orchestrate and log this precise production lifecycle:
1. Multi-Currency Operations: Post a trip as a 'CLERK' role with a foreign currency layout (e.g., gross amount in EUR at a 1.25 exchange rate) and watch the backend automatically split it into base currency net revenues and VAT liabilities.
2. Executive Authorization: Use a token carrying the 'CEO' role header to approve and digitally stamp the transaction, moving it into a status of 'POSTED_TO_MAIN_LEDGER'.
3. Cross-Currency Bank Clearing: Send a POST request to `/api/finance/bank-reconciliation/multi-currency` uploading a mock bank statement row. Pass the bank amount in USD and assert that the multi-currency reconciliation matcher calculates the exchange conversion correctly and clears the internal asset line to 'RECONCILED'.
4. Index Performance Audit: Trigger a GET request to `/api/admin/db/optimize-check` and confirm that our B-Tree and Composite indexes keep the query execution time under 50 milliseconds.

Please include explicit console logging print lines with colorful status tags ([PASS]/[FAIL]) so we can audit the results easily in the VS Code terminal logs.

Please write a final, comprehensive Python script using the 'requests' library that we can run from our VS Code terminal window to test our live backend. This will act as our final compliance verification suite to prove the full system works perfectly before we push to the cloud.

The simulation script should orchestrate and log this precise production lifecycle sequence:
1. Multi-Currency Operations: Post a trip payload as a 'CLERK' role with a foreign currency layout (e.g., gross amount in EUR at a 1.20 exchange rate). Confirm the backend automatically splits it into base currency net revenues and VAT liabilities across our grandchild accounts.
2. Executive Authorization: Use a token carrying the 'CEO' role header to approve, digitally sign, and stamp the transaction, moving it from a pending state into a status of 'POSTED_TO_MAIN_LEDGER'.
3. Cross-Currency Bank Clearing: Send a POST request to `/api/finance/bank-reconciliation/multi-currency` uploading a mock bank statement row. Pass the bank amount in USD and assert that the multi-currency reconciliation matcher calculates the exchange conversion correctly and clears the internal asset line to 'RECONCILED'.
4. Index Performance Audit: Trigger a GET request to `/api/admin/db/optimize-check` and confirm that our B-Tree and Composite indexes keep the query execution time under 50 milliseconds.

Please output the complete standalone python script with clear, colorful terminal print logs using status tags ([PASS]/[FAIL]) so we can audit the results easily in the VS Code terminal window.

Please refactor our entire backend package to remove all hardcoded references to "Meayon" and replace them with a dynamic, white-labeled corporate branding configuration.

1. CONFIGURATION LAYER: Update our database initialization to read global system variables from our environment configuration (.env), establishing these system-wide keys:
   - APP_SYSTEM_NAME="Onyx-Scale Enterprise ERP Core"
   - APP_COMPANY_NAME="Contracting and Logistics Co."
   - APP_DEFAULT_VAT_RATE=0.15

2. ACCOUNTING & SWAGGER INJECTION: Update `main.py` so that the OpenAPI/Swagger interface titles dynamically inject `os.getenv("APP_SYSTEM_NAME")`. Ensure that during database initialization (`database.py`), the default seeded account names are updated to match general commercial definitions (e.g., changing "Meayon Main Vault Cash" to a clean, universal string like "Main Operating Vault Cash").

3. DYNAMIC SETTINGS ENDPOINT (`GET /api/system/settings`): Create a new public endpoint that returns our branding settings as a clean, flat JSON object containing floats:
   { "system_name": str, "company_name": str, "vat_rate": float }
This will act as our central translation layer, allowing our available frontend UI dashboard charts, login screens, and tables to fetch and update their on-screen header titles and titles dynamically.

4. PDF ENGINE WRAPPER: Update our in-memory PDF generation code in `financial_reports.py` so that the print-ready document headers automatically inject `os.getenv("APP_COMPANY_NAME")` at runtime, ensuring all generated audit documentation carries the active company's commercial profile perfectly.

Please provide this updated implementation using our relative package structure paths and rerun our backend test suites to confirm absolute compilation stability.

# Global ERP White-Label Configuration Parameters
APP_SYSTEM_NAME="Onyx-Scale ERP Core"
APP_COMPANY_NAME="Universal Contracting & Logistics"
APP_DEFAULT_VAT_RATE=0.15

# Core Network Infrastructure Ports
API_BASE_URL="http://127.0.0.1:8000"
DATABASE_URL="sqlite:////app/data/meayon_erp.db"
NGINX_ACCESS_LOG="/var/log/nginx/access.log"

Please locate our primary configuration environment file (look for '.env' or '.env.example' in our workspace root directory). If '.env' does not exist yet, please create a new file named exactly '.env' directly in our root workspace path. 

Once found or created, add or completely replace its contents with the following commercial white-label configuration parameters:

# Global ERP White-Label Configuration Parameters
APP_SYSTEM_NAME="Oxen GL"
APP_COMPANY_NAME="Oxen Logistics & Contracting"
APP_DEFAULT_VAT_RATE=0.15

# Core Network Infrastructure Ports
API_BASE_URL="http://127.0.0.1:8000"
DATABASE_URL="sqlite:////app/data/oxen_gl.db"
NGINX_ACCESS_LOG="/var/log/nginx/access.log"

Please verify that the file is saved properly in our root directory so our settings module can read these variables dynamically on the next application startup.

Please expand our Commercial SaaS License system to support dynamic Multi-Tenant Visual Identities and Themes using our relative package structure paths.

1. DATABASE MODEL ENHANCEMENTS: Update our 'TenantLicense' model table in `models.py` to incorporate these specific user-interface layout attributes:
   - UI_THEME_MODE (String, default='LIGHT', CHECK constraint: 'LIGHT', 'DARK', 'CUSTOM')
   - UI_PRIMARY_COLOR (String, default='#1E3A8A', capturing a standard primary brand hex-code)
   - UI_SECONDARY_COLOR (String, default='#10B981', capturing a secondary accent hex-code)
   - UI_FONT_FAMILY (String, default='Inter, sans-serif')
   - UI_LOGO_URL (String, Nullable, storing a link to the tenant's custom dashboard logo image)

2. UPGRADE GLOBAL SETTINGS DISCOVERY ROUTE:
- Update our public setting retrieval pipeline (`GET /api/system/settings`) in `main.py` so it dynamically extracts these visual identity fields based on the incoming tenant context or fallback environment files.
- Return the complete visual profile object as flat, JavaScript-safe values:
  {
    "system_name": str,
    "company_name": str,
    "vat_rate": float,
    "theme_mode": str,
    "primary_color": str,
    "secondary_color": str,
    "font_family": str,
    "logo_url": str
  }

3. FRONTEND CONSUMPTION SAFEGUARD: Ensure that if an anonymous visitor or a new terminal simulator checks this route, it gracefully falls back to our neutral 'Oxen GL' environment parameters rather than throwing a processing crash, keeping our available browser UI components completely intact.

Please provide this updated implementation and run our backend verification suites to confirm compilation stability.

Please update our backend environment configuration logic and public settings route to support multiple built-in system default identities without altering tenant client asset overrides.

1. UPDATE ENVIRONMENT FALLBACKS (.env & settings.py):
- Update our global environment settings to store the original legacy "Meayon" design constants as the system's absolute baseline default:
  * APP_DEFAULT_THEME_MODE="LIGHT"
  * APP_DEFAULT_PRIMARY_COLOR="#0F172A" (or insert the exact custom hex-code your legacy Meayon theme used, e.g., deep charcoal, safety amber, etc.)
  * APP_DEFAULT_SECONDARY_COLOR="#F59E0B"
  * APP_DEFAULT_FONT_FAMILY="Inter, sans-serif"
  * APP_DEFAULT_LOGO_URL="https://example.com"

2. CONFIGURE "OXEN BLUE" AS A BUILT-IN ALTERNATIVE:
- Ensure the system configuration registers the clean "Oxen Blue" theme (#1E3A8A primary, #10B981 secondary) as an explicitly selectable alternate global option.

3. REFACTOR CONFIGURATION DISCOVERY PIPELINE (`GET /api/system/settings`):
- Update the public route logic in `main.py`. When an anonymous visitor loads the login or public landing screens (passing no 'X-Tenant-License-Key' header):
  * Check the system setting. By default, return the exact legacy "Meayon" visual identity parameters.
  * If a query parameter or environment toggle switches the global identity to 'oxen_blue', return the alternative '#1E3A8A' color space profile instead.
- If a valid tenant key IS supplied, completely bypass these system options and return their personalized client styling parameters exactly as stored in their license record, ensuring client branding remains fully protected.

Please provide this updated implementation and run our backend validation test suites to ensure compilation stability.

