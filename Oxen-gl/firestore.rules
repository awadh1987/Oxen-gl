rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 0 containment: no browser client may access ERP Firestore data.
    // Server-authoritative API access will replace this temporary deny-all rule.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
